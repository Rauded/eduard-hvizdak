// Czech dictionary. Present and selectable in the switcher, but intentionally
// empty for now: every key falls back to English until Czech translations are
// added. Fill in namespaces here the same way as sk/index.ts when ready.
import { Dict } from '../en';
import { DeepPartial } from '../types';

export const cs: DeepPartial<Dict> = {};
